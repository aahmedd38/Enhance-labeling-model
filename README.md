•	Automatically identifying and correcting obvious inconsistencies or errors in the AI-generated labels based on established patterns and hard-coded validation rules 
• HITL for identifies and flags ambiguous, complex, or borderline cases that cannot be confidently resolved by automated rules.
• These are cases where the AI's confidence score is low , These flagged cases are then escalated for human review .
